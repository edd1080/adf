# OpenCode adapter

OpenCode discovers the shared ADF skills under `.agents/skills/`; ADF does not create a duplicate `.opencode/skills/` tree. The `opencode.json` file is a merge fragment, and the Markdown agents provide read-only review and diagnosis roles.

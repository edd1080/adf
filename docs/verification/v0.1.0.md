# ADF v0.1.0 Verification

## Milestone M1 — CLI skeleton and core contracts

**Date:** 2026-08-11  
**Branch target:** `feat/adf-v0.1.0`  
**Result:** PASS

### TDD evidence

| Task   | RED evidence                                      | Expected cause                       | GREEN evidence  |
| ------ | ------------------------------------------------- | ------------------------------------ | --------------- |
| Task 1 | `npm test -- tests/unit/cli.test.ts`              | `src/cli.ts` did not exist           | 1 test passed   |
| Task 2 | `npm test -- tests/unit/domain-contracts.test.ts` | Domain modules did not exist         | 10 tests passed |
| Task 3 | `npm test -- tests/unit/infrastructure.test.ts`   | Infrastructure modules did not exist | 6 tests passed  |

### Batch verification

| Check            | Command                | Result                            |
| ---------------- | ---------------------- | --------------------------------- |
| TypeScript       | `npm run typecheck`    | PASS                              |
| ESLint           | `npm run lint`         | PASS                              |
| Tests            | `npm test`             | PASS — 3 files, 17 tests          |
| Build            | `npm run build`        | PASS                              |
| Formatting       | `npm run format:check` | PASS                              |
| Dependency audit | `npm install`          | PASS — 0 vulnerabilities reported |

### Scope delivered

- TypeScript ESM CLI skeleton with `init`, `doctor`, `status`, `next`, and `update` command names.
- Runtime contracts for manifest, project state, diagnostics, and deterministic file actions.
- Editor JSON schemas for manifest and state.
- Safe relative-path validation and early gate invariants.
- Injectable filesystem, process runner, hashing, and clock ports.
- Approved design and implementation plan preserved under `docs/`.

### Explicitly not performed

- No global or project installation of ADF.
- No Spec Kit initialization.
- No target application generated.
- No commit, push, tag, release, or npm publication.

## Milestone M2 — Safe bootstrap engine

**Date:** 2026-08-11  
**Result:** PASS

### TDD evidence

| Task   | RED evidence                                               | Expected cause                   | GREEN evidence |
| ------ | ---------------------------------------------------------- | -------------------------------- | -------------- |
| Task 4 | `npm test -- tests/unit/project-inspector.test.ts`         | Project inspector did not exist  | 6 tests passed |
| Task 5 | `npm test -- tests/unit/template-registry.test.ts`         | Template registry did not exist  | 5 tests passed |
| Task 6 | `npm test -- tests/unit/change-planner.test.ts`            | Change planner did not exist     | 7 tests passed |
| Task 7 | `npm test -- tests/integration/transaction-writer.test.ts` | Transaction writer did not exist | 5 tests passed |
| Task 8 | `npm test -- tests/unit/spec-kit-adapter.test.ts`          | Spec Kit adapter did not exist   | 7 tests passed |

### Batch verification

| Check           | Command                | Result                   |
| --------------- | ---------------------- | ------------------------ |
| TypeScript      | `npm run typecheck`    | PASS                     |
| ESLint          | `npm run lint`         | PASS                     |
| Full test suite | `npm test`             | PASS — 8 files, 47 tests |
| Build           | `npm run build`        | PASS                     |
| Formatting      | `npm run format:check` | PASS                     |

### Scope delivered

- Read-only greenfield/brownfield inspection with documentation metadata, Git state, agent adapters, Spec Kit state, file digests, and symlink escape diagnostics.
- Embedded, versioned template registry with explicit variables and deterministic hashes.
- Pure, deterministic change planning with create, merge, preserve, conflict, and no-op previews.
- Idempotency behavior when generated bytes already match.
- Transactional staging, managed-block merge, rollback after injected failure, target containment, and symlink refusal.
- Spec Kit capability detection through documented commands and `.specify/integration.json` fallback.
- Operation proposals that keep command arguments separated, never add `--force`, and preserve an existing default integration.
- Local-template fallback when bundle or workflow capabilities are unavailable.

### Regression caught by the gate

The generic formatter interpreted Markdown Handlebars templates as HTML and collapsed headings and lists. Template-content tests failed before synchronization. The templates were restored and `templates/**/*.hbs` is now excluded from generic Prettier formatting; their structure remains protected by registry tests.

### Explicitly not performed

- No ADF installation or target project initialization.
- No real Spec Kit command executed against a project.
- No commit, push, tag, release, or npm publication.

## Milestone M3 — Skills and agent adapters

**Date:** 2026-08-11  
**Result:** PASS

### TDD evidence

| Task    | RED evidence                                                                            | Expected cause                                    | GREEN evidence           |
| ------- | --------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------ |
| Task 9  | `npm test -- tests/unit/skill-registry.test.ts tests/static/skill-dependencies.test.ts` | Skill registry and vendored sources did not exist | 3 files, 7 tests passed  |
| Task 10 | `npm test -- tests/behavior/skill-routing.test.ts tests/static/core-skills.test.ts`     | Core skills and routing exports did not exist     | 2 files, 13 tests passed |
| Task 11 | `npm test -- tests/unit/agent-adapters.test.ts`                                         | Agent adapter service did not exist               | 1 file, 5 tests passed   |

### Batch verification

| Check                  | Command / method                    | Result                    |
| ---------------------- | ----------------------------------- | ------------------------- |
| TypeScript             | `npm run typecheck`                 | PASS                      |
| ESLint                 | `npm run lint`                      | PASS                      |
| Full test suite        | `npm test`                          | PASS — 14 files, 72 tests |
| Build                  | `npm run build`                     | PASS                      |
| Formatting             | `npm run format:check`              | PASS                      |
| Vendor reproducibility | regenerate and compare every digest | PASS — 18 files identical |

### Pinned upstream

- Repository: `https://github.com/mattpocock/skills.git`
- Commit: `84fdeffd12f2ee307994d1eb6feb48173b6e0502`
- License: MIT, copied verbatim to `templates/skills/vendor/LICENSE.mattpocock-skills`
- Frozen allowlist: `grilling`, `grill-with-docs`, `domain-modeling`, `codebase-design`, `tdd`, `diagnosing-bugs`, `code-review`, and `writing-great-skills`
- Upstream rename recorded explicitly: `writing-for-agents` → ADF stable name `writing-great-skills`

### Scope delivered

- Maintainer-only vendoring command with an exact allowlist, commit validation, strict upstream path layouts, license validation, source/adapted SHA-256 values, dependencies, and adaptation notes.
- Portable Agent Skills frontmatter limited to `name`, `description`, `license`, and string-valued `metadata`.
- ADF path adaptations for `docs/product/glossary.md` and `docs/decisions/`, sequential fallbacks when subagents are unavailable, explicit Git authorization, and final file inventories.
- Seven lifecycle skills: `project-intake`, `session-start`, `context-router`, `bug-fix`, `verify-work`, `feature-close`, and `session-end`.
- Deterministic positive and negative trigger fixtures, including PR review not invoking intake and a typo not entering feature or bug-fix closure.
- Shared Codex/OpenCode contract through root `AGENTS.md` and `.agents/skills/`.
- OpenCode merge fragment with skill discovery, approval boundaries, preserved user models/providers/agents, and read-only reviewer/debugger roles.
- Codex adapter with no speculative `.codex/config.toml`.

### Formatting boundary

`templates/skills/**` is excluded from generic Prettier formatting. Vendored prose and provenance hashes are generated deterministically by `scripts/vendor-skills.mjs` and validated by tests instead of being rewritten by a generic formatter.

### Explicitly not performed

- No skill was installed globally or into a target project.
- No OpenCode or Codex adapter was applied to a target project.
- No Spec Kit command was executed against a target project.
- No commit, push, tag, release, deployment, or npm publication.

## Milestone M4 — User commands

**Date:** 2026-08-11  
**Result:** PASS

### TDD evidence

| Task    | RED evidence                                                                                 | GREEN evidence                                                                                                                |
| ------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Task 12 | `tests/integration/init-command.test.ts` failed while command modules and wiring were absent | Install, dry-run, decline, conflict, missing Spec Kit, multi-agent preview, preview ordering and brownfield confirmation pass |
| Task 13 | `tests/integration/read-commands.test.ts` failed while read commands were absent             | `doctor`, `status` and `next` pass with zero writes                                                                           |
| Task 14 | `tests/integration/update-command.test.ts` failed while updater was absent                   | Check-only, upgrade, conflict, orphan, provenance and managed `AGENTS.md` update pass                                         |

### Scope delivered

- CLI ejecutable con preview anterior a la aprobación y escritura.
- JSON final estable; el preview de una ejecución `--json` se dirige a stderr.
- Bootstrap de Spec Kit anterior a la escritura ADF, sin `--force` silencioso.
- Brownfield no interactivo bloquea antes de escribir cuando Spec Kit necesita confirmar el merge.
- Actualizaciones muestran preview, solicitan confirmación y vuelven a planificar antes de aplicar.
- Los fallos de Spec Kit reportan archivos creados, modificados o eliminados que pudieron observarse.

## Milestone M5 — Paquete Spec Kit

**Date:** 2026-08-11  
**Result:** PASS con capacidad detectada

| Check                | Evidencia                                                           | Resultado                                                                  |
| -------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Workflow estructural | `tests/static/spec-kit-package.test.ts`                             | PASS: IDs estables, gates G1–G4, cero shell, cero `speckit.implement`      |
| Fuentes locales      | `tests/integration/spec-kit-package.test.ts`                        | PASS: preset, workflow, step y bundle instalados bajo `.harness/spec-kit/` |
| Workflow real        | `specify workflow list` en clean-room real                          | `adf-day-zero v0.1.0` instalado                                            |
| Preset real          | `specify preset list` en clean-room real                            | `adf-guardrails v0.1.0` habilitado                                         |
| Bundle real          | `specify bundle validate --path .harness/spec-kit/bundle --offline` | Bien formado; warning esperado para step no publicado en catálogo          |

La instalación local detectada fue Spec Kit `0.13.3.dev0`. Esa versión no expone `specify workflow validate`; ADF no inventó el comando. La validación ocurrió mediante tests estructurales y una instalación real con `workflow add --dev`.

## Milestone M6 — Release candidate local

**Date:** 2026-08-11  
**Result:** PASS

### Clean-room

| Escenario           | Evidencia                                    | Resultado                                                                   |
| ------------------- | -------------------------------------------- | --------------------------------------------------------------------------- |
| Greenfield simulado | `tests/clean-room/day-zero-flow.test.ts`     | Init → doctor → next G1, sin código de producto                             |
| Brownfield          | mismo archivo                                | Código, brief y reglas locales preservados; segunda ejecución sin writes    |
| Idempotencia        | mismo archivo + clean-room real              | Segunda instalación produce acciones `noop`/`preserve` y cero escritura ADF |
| Cross-agent         | mismo archivo                                | Codex/OpenCode comparten `AGENTS.md`, estado y un único skill set           |
| Cuatro intakes      | `tests/unit/day-zero-router.test.ts`         | Vacío, parcial, completo y brownfield enrutan a `project-intake`            |
| Transcript G1–G4    | `tests/behavior/day-zero-transcript.test.ts` | Gates humanos explícitos y stop antes de implementación                     |
| Package boundary    | `tests/clean-room/npm-pack.test.ts`          | Runtime completo; tests, fixtures, `.git`, `.harness` y secretos excluidos  |

### Clean-room real con Spec Kit

Directorio aislado: `work/actual-cleanroom-20260811-b` fuera del repositorio fuente ADF.

1. `node dist/cli.js init TARGET --agent codex --yes --json` → `installed`.
2. El plan final preservó la constitución creada por Spec Kit y creó el harness ADF.
3. `adf doctor --json` → `healthy: true`, `DOCTOR_OK`.
4. `adf next --json` → `Inicia el proyecto.`.
5. `specify workflow list` y `specify preset list` confirmaron las fuentes ADF activas.
6. Segunda ejecución real → `installed`, sin reinstalar preset/workflow ni reescribir archivos ADF.

## Revisión de dos ejes

La primera revisión final detectó gaps reales que la simulación no cubría. Se corrigieron antes de declarar el release candidate:

- Bootstrap real de Spec Kit reordenado antes de la escritura ADF.
- Preview visible antes de confirmación/escritura en init y update.
- Stdin interactivo solo cuando el usuario debe aprobar el merge de Spec Kit.
- Idempotencia real de preset/workflow y de `AGENTS.md` brownfield.
- Update del bloque gestionado de `AGENTS.md` sin perder reglas del usuario.
- Reporte observable de mutaciones parciales de Spec Kit.
- Transcripts Day Zero y matriz M4–M6 agregados.

## Matriz de los 15 criterios de aceptación

| #   | Criterio                                                  | Evidencia                                                        | Estado                         |
| --- | --------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------ |
| 1   | Instalar en directorio vacío con un comando               | Clean-room real `init --yes`                                     | PASS                           |
| 2   | Preview antes de primera escritura                        | Test de orden `preview → spec-kit`; callback CLI previo          | PASS                           |
| 3   | Doctor confirma o explica gaps                            | Read-command tests + clean-room real `DOCTOR_OK`                 | PASS                           |
| 4   | Codex/OpenCode comparten estado                           | Cross-agent clean-room                                           | PASS                           |
| 5   | `Inicia el proyecto` activa intake                        | Router, `next` y transcript                                      | PASS                           |
| 6   | Descubrir documentos antes de preguntar                   | Skill `project-intake` + transcript                              | PASS                           |
| 7   | Brief, PRD y flujos sobreviven sesiones                   | Templates, session skills y handoff tests/static contracts       | PASS                           |
| 8   | G1 requiere humano                                        | Workflow gate + transcript actor `human`                         | PASS                           |
| 9   | Primera feature usa Spec Kit                              | Workflow usa `speckit.specify`, `plan` y `tasks`                 | PASS                           |
| 10  | G4 bloquea código hasta `GO`                              | Gate G4; workflow no contiene implementación                     | PASS                           |
| 11  | Sin commit/push implícito                                 | Static skill/adapters tests y ausencia de comandos Git mutantes  | PASS                           |
| 12  | Reinstalación idempotente                                 | Greenfield, brownfield y clean-room real                         | PASS                           |
| 13  | Conflicto preserva archivo                                | Init/update/transaction tests                                    | PASS                           |
| 14  | Skills fijadas y atribuidas                               | Registry, dependency, hash y license tests                       | PASS                           |
| 15  | Bootstrap → intake → G1 → feature-ready sin pasos ocultos | Clean-room real hasta G1 + transcript/workflow contractual G1–G4 | PARTIAL — UAT humana pendiente |

El criterio 15 tiene evidencia técnica suficiente para un release candidate, pero no se declara completo: falta ejecutar una sesión clean-room literal con Codex u OpenCode y aprobaciones humanas reales en G1–G4. El transcript determinista verifica el contrato; no sustituye esa UAT.

## Release-candidate gate final

| Check                                  | Resultado                                             |
| -------------------------------------- | ----------------------------------------------------- |
| `npm run verify`                       | PASS — 25 archivos, 121 tests; 1 release test omitido |
| TypeScript / ESLint / build / Prettier | PASS                                                  |
| Paquete npm dry-run                    | PASS                                                  |
| `npm run test:release`                 | PASS — tarball y ambos shims npm desde caché limpia   |
| Publicación                            | NO ejecutada                                          |

El gate técnico puede quedar verde, pero la publicación estable sigue condicionada a la UAT humana del criterio 15.

### Decisiones aún separadas

- Publicación y verificación de `adf-harness-kit@0.1.0` en npm.
- Tag, firma, checksums y GitHub Release.
- Política de publicación del bundle en catálogo Spec Kit.
- Soporte formal de Windows.

El resultado de commit, push, tag, release y publicación npm debe actualizarse únicamente con evidencia del sistema externo.
